# Core runtime
PySide6>=6.7
uiautomator2>=3.0.0
requests>=2.31.0

# Optional feature dependencies
# Used by OCR-based Facebook profile reading
rapidocr-onnxruntime

# Used by browser automation when resolving Facebook share links
selenium
webdriver-manager

# Development and packaging
Pillow
pyinstaller
